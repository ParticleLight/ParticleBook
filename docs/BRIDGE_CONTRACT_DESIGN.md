# M7 桥接契约：类型级生成 方案设计

> **状态：已按 §1–§5 落地（§6 可选加固未做，留待决策）。**
> 交付：`bridge/contract.mjs`（92 条权威表）、`scripts/gen-bridge-dts.mjs`（生成 + `--check` 新鲜度）、
> `scripts/check-bridge-contract.js` 扩展为四条断言（成员名 / **参数元数** / **桥接方法名** / 表完整性）。
>
> 验收实测：生成物与改动前手写版 **语义等价**（92 成员逐一比对签名，0 差异）；`tsc` 0 错误；
> `build` / 7 单测 / i18n / 版本全通过；**干净克隆 13 步全绿**；三条变异测试均精确变红。
>
> 执行中发现并修复的两个环境问题（都已验证）：
> ① 本仓库 `core.autocrlf=true` 且无 `.gitattributes`，全新检出为 CRLF 而生成器输出 LF —— 
>    `--check` 会在 CI 上必然误报。已改为比对前归一化行尾（校验的是内容而非行尾字节）。
> ② 生成器写 LF 会让 git status 长期显示修改（而 `git diff` 为空），因为磁盘形态不是 git 期望的。
>    已改为保持既有行尾、缺失时向 git 询问期望行尾，并在内容等价时跳过写入（幂等）。
> 状态：**设计稿，待确认后再动代码。** 依据：对存根 / d.ts / C++ 注册三方的实测侦察。
